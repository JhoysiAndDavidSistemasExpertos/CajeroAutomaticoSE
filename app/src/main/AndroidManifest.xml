<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.davidgj.cajeroautomaticose">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:name=".Principal"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".codigochip.ui.LoginChipActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".codigopin.ui.LoginPinActivity"></activity>
        <activity android:name=".usermenu.ui.MenuActivity"></activity>
        <activity android:name=".transactiondinero.TransactionDineroActivity"></activity>
        <activity android:name=".transationsaldo.TransactionSaldo"></activity>
    </application>

</manifest>